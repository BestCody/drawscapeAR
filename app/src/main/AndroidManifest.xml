<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Camera is required for AR -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Internet for Cloud Anchors + Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Declare that this app requires ARCore -->
    <uses-feature android:name="android.hardware.camera.ar" android:required="true" />

    <application
        android:name=".DrawscapeApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Eureka">

        <!-- Required: tells ARCore this app uses it -->
        <meta-data
            android:name="com.google.ar.core"
            android:value="required"
            tools:replace="android:value" />

        <!-- Required: Cloud Anchor / ARCore API key -->
        <meta-data
            android:name="com.google.android.ar.API_KEY"
            android:value="${ARCORE_API_KEY}" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Eureka">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>